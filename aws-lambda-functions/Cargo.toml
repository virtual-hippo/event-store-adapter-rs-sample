[package]
name = "aws-lambda-functions"
version.workspace = true
edition.workspace = true
description = "AWS Lambda functions for the Event Store Adapter"

[dependencies]
lambda_http.workspace = true
tokio.workspace = true
